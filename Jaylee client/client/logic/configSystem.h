#pragma once

// Initialize the config system: creates folders/files if not exist, or loads settings.
void LoadSettingsFromConfig();

// Save all settings to config file.
void SaveSettingsToConfig();
